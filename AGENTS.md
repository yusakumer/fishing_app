# fishing_app: Codex 作業ルール

## 役割と進め方

- ユーザーは学習を目的に自分で実装する。コード、設定ファイル、Dockerfile、Composeファイルは、明示的に編集を依頼された場合にだけ変更する。
- 技術的な作業を案内するときは、目的、コマンドや設定の意味、実行後の確認方法を日本語で説明する。
- 先回りして実装手順の詳細や完成コードを提示しすぎず、ユーザーが考えたり実装したりできる余地を残す。質問された範囲を中心に答える。
- 設計・進捗はNotionの「実装計画・進捗管理」を最新の参照先として扱う。Notionの内容と会話内容に差異がある場合は、確認してから判断する。
- Docker環境、認証、機能単位などの区切りで、コミット候補のファイル・確認済み内容・推奨コミットメッセージを提案する。コミットやpushの実行は、ユーザーが明示的に依頼した場合だけ行う。
- 機能の区切りでは、RSpec等の自動テストとPostman等による手動API確認のどちらを行うか、または両方行うかを提案する。
- 新しい作業を案内する前に、目的、なぜ今行うのか、影響範囲、完了確認の方法を説明する。ユーザーの理解に不明点が残る場合は、次の作業へ進む前に補足・修正する。

## 現在の技術方針

- バックエンド: Rails API
- フロントエンド: React + Vite
- 開発環境: Docker Compose
- データベース: PostgreSQL
- 認証: Devise + devise-jwt
- JWTの無効化: Redisで失効済みJWTを管理する
- 非同期処理: Sidekiq + Redisを使用予定
- JWTのフロントエンド保管先: HttpOnly Cookieを使用する

## 情報の確実性

- 技術的な事実、バージョン依存の仕様、セキュリティ、設定方法は、公式ドキュメント・公式リポジトリ・一次情報を優先して確認する。
- 信頼できる一次情報で確認できない内容は、断定しない。不確実であること、確認できていない点、確認方法を明示する。
- 外部の記事や技術ブログの情報を補助として使うことはよいが、公式情報と区別して扱う。記事だけを根拠に重要な設定や安全性の判断を確定しない。
- 回答内で推測や一般的な慣例を述べる場合は、その旨を明示する。

## 安全な変更

- 既存ファイルやデータを削除・上書きする操作は、対象と影響を説明し、ユーザーの明確な依頼または承認がある場合のみ行う。
- Docker、DB、Redisのデータ初期化やボリューム削除は、データが消えることを明示してから案内する。
